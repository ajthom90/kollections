kotlin.code.style=official
kotlin.js.generate.executable.default=false
# Silently skip native targets that can't be built on the current host instead of failing.
kotlin.native.ignoreDisabledTargets=true
